# Placeholder __PORT__ replaced at container start (Railway sets PORT).
server {
    listen __PORT__;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        try_files $uri /index.html =404;
    }

    location = /health {
        default_type text/plain;
        return 200 'ok';
    }
}